import org.junit.Test;
import org.junit.runner.RunWith;
import org.mockito.junit.MockitoJUnitRunner;

@RunWith(MockitoJUnitRunner.class)
public class QuizScreenLayoutTester {
	@Test
	public void testSkipAndNextButtonClickabilityChangesAfterASelectionIsMade() {
		// QuizScreenLayout testObject = new
		// QuizScreenLayout(Mockito.mock(MainStage.class));

	}

}